name: Export Kinetra full development environment

on:
  pull_request:
    branches: [develop]
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  export:
    if: github.head_ref == 'feature/t04-profile-survey'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - name: Upload repository with complete dependencies
        uses: actions/upload-artifact@v4
        with:
          name: kinetra-t04-full-env
          path: |
            .
            !.git/**
            !apps/frontend/dist/**
            !apps/backend/dist/**
            !packages/shared/dist/**
          include-hidden-files: true
          compression-level: 0
          retention-days: 1
