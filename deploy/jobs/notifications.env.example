# Dedicated notifications credentials. Never copy the API environment here.
NODE_ENV=production
DATABASE_URL=postgresql://kinetra_job:REPLACE_SECRET@database.example.invalid:5432/kinetra?sslmode=verify-full
VAPID_PUBLIC_KEY=REPLACE_PUBLIC_KEY
VAPID_PRIVATE_KEY=REPLACE_PRIVATE_KEY
VAPID_SUBJECT=mailto:ops@example.invalid
